name: Period Heath Check

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '*/5 * * * *'
jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: URL Health Check
        uses: Jtalk/url-health-check-action@v5
        with:
          url: https://fs-pokedex-db88.onrender.com|https://fs-pokedex-db88.onrender.com/health
